def reverse(text):
    """Return the reverse of the given string.

    Parameters:
        text (str): The string to reverse.

    Returns:
        str: The reversed string.
    """
    return text[::-1]